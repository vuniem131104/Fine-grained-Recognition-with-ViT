apiVersion: v1
kind: Service
metadata:
  name: bird-classification-frontend
  namespace: {{ .Values.namespace }}
  labels:
    app: bird-classification-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    app: bird-classification-frontend
